{
  "name": "istanbul-report-server",
  "version": "1.0.0",
  "description": "An Express server for showing istanbul coverage reports",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adam Wong",
  "license": "ISC",
  "dependencies": {
    "istanbul-middleware": "^0.2.2"
  }
}
